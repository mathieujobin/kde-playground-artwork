1) Drag and drop oxygen.jar onto the extensions dialog in firefox
2) Restart Firefox


Update the jar:
$ cd Oxygen
$ zip -r ../oxygen.jar *

Note that before you commit an update, make sure the jar doesn't include
Oxygen's .svn folders.

